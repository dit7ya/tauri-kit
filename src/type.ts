  export type CommandEntry = {
      description: string;
      route?: string;
      command?: string;
      key: string; // TODO can use an enum
  };

  // export type GoToRoute = {
  //     route: string;
  // };

  // export type Execute = {
  //     command: string;
  // };
